name: Action 2

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Here goes the shared code

      - name: Run script specific to action 2
        run: echo "this is action 2"
